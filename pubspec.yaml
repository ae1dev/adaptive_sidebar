name: adaptive_sidebar
description: "Sleak sidebar for responsive Flutter apps with automatic size change."
version: 0.7.0
homepage: https://github.com/ae1dev/adaptive_sidebar

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - ui
  - navigation
  - layout
  - scaffold

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  universal_io: ^2.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
